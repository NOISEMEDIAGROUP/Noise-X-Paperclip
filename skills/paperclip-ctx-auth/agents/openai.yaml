interface:
  display_name: "Paperclip ctx auth"
  short_description: "Authenticate local Paperclip API calls in ctx_execute"
  default_prompt: "Use $paperclip-ctx-auth to call the local Paperclip API from ctx_execute."
