apiVersion: v1
kind: ConfigMap
metadata:
  name: paperclip-config
data:
  PAPERCLIP_DEPLOYMENT_EXPOSURE: "private"
  PAPERCLIP_RATE_LIMIT_PER_MINUTE: "240"
  PAPERCLIP_METRICS_ALLOW_ANONYMOUS: "true"
