# 2026-03-15 - Daily Improvement Workflow Bootstrap

## Repo Context Inspected

- docs reviewed: `doc/GOAL.md`, `doc/PRODUCT.md`, `doc/SPEC-implementation.md`, `doc/DEVELOPING.md`
- workflows inspected: company-scoped control-plane flows for companies, agents, issues, goals, approvals, costs, and plugins
- actors and auth inspected: implicit local board, authenticated Better Auth board sessions, bearer agent API keys, local agent JWTs, trusted-origin mutation guard
- routes and services inspected: `/api` route registration in `server/src/app.ts`, route exports in `server/src/routes/index.ts`, shared API constants in `packages/shared/src/api.ts`
- types, validators, and schema inspected: shared types in `packages/shared/src/types/*`, schema modules in `packages/db/src/schema/*`
- UI workflows or autosave inspected: issue document autosave and comment draft persistence

## Safe Action

- goal: create a durable operating contract for a daily Paperclip improvement agent
- shipped change: added a grounded daily prompt document and reusable changelog template in `doc/plans/`
- why it was safe: documentation-only change plus a script alias, no runtime behavior changed

## Moonshot

- goal: create a self-improving daily engineering loop that can safely extend Paperclip over time
- shipped change or foundation slice: defined the prompt contract for sub-agent inspection, safe-action plus moonshot delivery, repo invariants, and daily reporting
- long-term payoff: enables iterative autonomous improvements without drifting away from product scope or engineering discipline
- why this still stayed in scope: it strengthens Paperclip's own development process without adding dependencies or changing product boundaries

## Files Changed

- `package.json`
- `AGENTS.md`
- `doc/DEVELOPING.md`
- `doc/plans/2026-03-15-daily-improvement-agent-prompt.md`
- `doc/plans/2026-03-15-daily-improvement-log-template.md`
- `doc/plans/2026-03-15-daily-log.md`

## Verification

- `pnpm lint`: failed due to an existing server typecheck issue resolving `hermes-paperclip-adapter/server` and `hermes-paperclip-adapter`
- `pnpm -r typecheck`: failed for the same existing server adapter resolution issue
- `pnpm test:run`: failed in this environment due to existing adapter import failures plus multiple tests that attempt socket binds and hit `listen EPERM`
- `pnpm build`: failed due to the existing server typecheck issue resolving `hermes-paperclip-adapter/server` and `hermes-paperclip-adapter`

## Coverage Status

- touched implementation coverage: not measured, because the repository does not currently expose a working no-new-dependency coverage gate
- repo-wide coverage status: not enforceable from current root scripts
- any enforcement blocker: no stable coverage command is wired today; additional work is needed before repo-wide 100% coverage can be claimed honestly

## Follow-ups

- next likely improvement: add an existing-tooling coverage workflow if one can be enabled without new dependencies
- risks or unanswered questions: decide whether `hermes-paperclip-adapter` is optional, missing from install, or needs a local type shim or conditional registration pattern

---

## Run 2 - Approval Workflow Drafts And Board Response Notes

### Repo Context Inspected

1. Product and workflows
   Paperclip remains a company-scoped control plane, not a general chat app; approvals, issues, and comments are the core collaboration surface and operator actions need clear audit context.

2. Actors, auth, and permissions
   `server/src/middleware/auth.ts` resolves implicit local board, authenticated board sessions, and agent credentials; `server/src/middleware/board-mutation-guard.ts` keeps board mutations on trusted browser origins outside local-trusted mode.

3. APIs, routes, and orchestration
   `server/src/app.ts` mounts `/api` routes centrally; `server/src/routes/approvals.ts` already supports `decisionNote` on approve, reject, and request-revision plus company access checks and activity logging.

4. Types, interfaces, validators, and schema
   Shared approval types in `packages/shared/src/types/approval.ts` and the approval API client in `ui/src/api/approvals.ts` already match the existing note-carrying server contract, so this run stayed additive at the UI/workflow layer.

5. UI workflows and persistence
   `ui/src/components/CommentThread.tsx` already persists issue comment drafts locally, while `ui/src/pages/ApprovalDetail.tsx` had no draft persistence and no board-response composer despite rendering approval comments and actions.

### Safe Action

- goal: make approval comment entry resilient to accidental refreshes and navigations
- shipped change: added per-approval local draft persistence for approval comments, aligned to the existing issue-comment autosave cadence
- why it was safe: UI-only additive change with no API or schema impact

### Moonshot

- goal: make approval decisions more legible and actionable for requesting agents
- shipped change or foundation slice: added a board response composer on approval detail that persists a local decision note draft and passes it through approve, reject, and request-revision actions
- long-term payoff: operators can leave explicit guidance on approvals without inventing ad hoc follow-up comments or losing decision context
- why this still stayed in scope: it deepens the existing approval workflow using an already-supported server contract

### Files Changed

- `ui/src/pages/ApprovalDetail.tsx`
- `ui/src/lib/approval-detail.ts`
- `ui/src/lib/approval-detail.test.ts`

### Tests And Verification Run

- `pnpm test:run ui/src/lib/approval-detail.test.ts`: failed because `vitest` was not installed in this worktree (`node_modules` missing)
- `pnpm --filter @paperclipai/ui typecheck`: failed because `tsc` was not installed in this worktree (`node_modules` missing)
- `pnpm lint`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment
- `pnpm -r typecheck`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment
- `pnpm test:run`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment
- `pnpm build`: not run after the targeted verification blocker was confirmed; expected to fail for the same missing-install reason in this environment

### Coverage Status

- touched implementation coverage: helper logic added in `ui/src/lib/approval-detail.ts` is covered by `ui/src/lib/approval-detail.test.ts`, but the test could not be executed because dependencies are not installed in this worktree
- repo-wide coverage status: not enforceable from current root scripts
- any enforcement blocker: missing workspace install prevents running existing test tooling, and there is still no dedicated coverage command wired at repo root

### Blockers Or Follow-ups

- install workspace dependencies before claiming verification on this run
- if the approval workflow keeps growing, extract the approval comment panel into a shared rich-thread surface rather than keeping a page-local implementation
